import javax.swing.JPanel;

public class InfoFormMain {

	public static void main(String[] args) {
		// Create main window (JFramme)
		JFrame = new JFrame ("Personal Information Frame");
		frame.setSize(400, 400);
		frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
		frame.setLayout(newBorderLayout(10, 10));
		
		// Create a panel for the form
		JPanel formPanel = new JPanel();
		formPanel.setLayout(new GridLayout(5,2,5,5));
		formPanel.setBorder(BorderFactory.createEmptyBorder(10,10,10,10));
		
		// Add labels and text fields
		Jlabel nameLabel = new Jlabel("Full Name:");
		JTextField nameField = new JTextField();
		JLabel emailLabel = new JLabel("Email:");
		JLabel ageLabel = new JLabel("Age:");
		JTextField ageField = new JTextField();
		JLabel genderLabel = new JLabel ("Gender:");
		String[] genders {"male",  "Female", "Other"};
		JTextArea bioArea = JTextArea(3,20);
		JScrollPane bioScroll = new JScrollPane(bioArea);
		

	}

}
